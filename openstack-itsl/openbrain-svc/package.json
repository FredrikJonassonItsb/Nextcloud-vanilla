{
  "name": "openbrain-svc",
  "version": "1.0.0",
  "private": true,
  "description": "Per-brain Open Brain MCP service for the ITSL Open Stack. Vendored from OB1's self-hosted Postgres server (integrations/kubernetes-deployment/index.ts), ported Deno -> Node 22, with Bearer auth, /ingest, write firewall and embedding pending-mode per CONTRACTS.md section 5.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@hono/mcp": "^0.1.5",
    "@hono/node-server": "^1.19.11",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "hono": "^4.9.2",
    "pg": "^8.16.0",
    "zod": "^3.25.76"
  }
}
