# Shell scripts and SQL must stay LF — they are executed on Linux (dev15)
# after tar-over-ssh from a Windows checkout.
*.sh   text eol=lf
*.sql  text eol=lf
Caddyfile text eol=lf
*.yml  text eol=lf
*.json text eol=lf
