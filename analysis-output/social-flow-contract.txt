HEADLINE: One engine, config-driven: build the 2 declared hooks + close 1 fail-open commit-modul default + 1 missing fas-spärr; defer attach-rewiring and partsModell-ACL to human decisions.

=== IMPLEMENT_NOW (6) ===

[HOOK-INFRA] (blocker) Add a data-driven hook-dispatch mechanism (preSagaHook + postCommitHook) — no per-kategori branches
  LOC: hubs_arende/lib/Service/ArendeService.php (constructor + new private dispatchHook(); wired in createCase and commit)
  EXPECT: Inject the DI-registered EdiariumPort as a trailing nullable/isAvailable-gated dependency into ArendeService (same pattern as the other optional ports). Add ONE small hook dispatcher keyed by hook-id string -> callable (a map, NOT an if(kategori===N) branch), so both kat6 preSagaHook='diariefor_direkt' and kat8 postCommitHook='familjeratt_yttrande' plug into the same mechanism. The dispatcher reads the hook id from the ArendeTyp config row (typ->getPreSagaHook()/getPostCommitHook()), and is a no-op when the id is null/empty or unknown (honest: log unknown ids, never throw on an unrecognised id). This is the shared substrate the spec's 'one engine, N config-rows, ~3 hooks' thesis requires; RT-1 and FAM-1 are the two consumers.
  VERIFY: phpunit: a unit test that registers a fake hook id in the map and asserts dispatchHook invokes exactly the matching callable and is a silent no-op for null/unknown ids. Confirm ArendeService still constructs when EdiariumPort is null (positional/no-DI harness).
  DEPENDS: -

[RT-1] (blocker) kat6 preSagaHook 'diariefor_direkt' is never dispatched — reverse-order diarieföring is dead config
  LOC: hubs_arende/lib/Service/ArendeService.php::createCase (insert dispatch AFTER R0/idempotency/typ-resolution/authz at ~line 219, BEFORE R1 mint/R2 insert)
  EXPECT: Before the R2 register INSERT, dispatch the type's preSagaHook via HOOK-INFRA. For 'diariefor_direkt' call EdiariumPort::registrera($hubsCaseId, [handlingstyp => typ->getDhpHandlingstyp() ('lvu_lvm_akt'), riktning => 'upprattad', arendetyp => 'rattsligt_tvang', inkomDatum, sekretess]) as led-0 and capture the diarienummer + provenanceState='registrerad' from the kvitto. buildEntity then seeds dnr + provenanceState='registrerad' for this path (the case is born already-registered, omvänd ordning mot kat1). Push a best-effort compensating closure onto the saga compensation stack so a later saga failure logs/flags the orphaned diarieföring (do not hard-fail the case on a compensation miss). If EdiariumPort is unavailable, fail-closed (throw) for a type whose ONLY distinguishing behaviour is the direct diarieföring — do not silently create the case unregistered. Note the idempotent compose: because provenanceState is born 'registrerad', a later commit() short-circuits to receiptFromRegistered() (line 1389) and never double-registers — verify this composition holds.
  VERIFY: Smoke: extend Smoke.php with arendeTyp='rattsligt_tvang' — create, then assert EdiariumStub::getDiarium(hubsCaseId) has exactly one handling, the case's provenanceState='registrerad' and dnr is set at BIRTH (before any commit), and that a subsequent commit() is the idempotent no-op (no second diarium entry). phpunit: assert createCase dispatches registrera before insert (ordering) and that an EdiariumPort=null causes a clean fail-closed exception, not a half-built case.
  DEPENDS: HOOK-INFRA

[FAM-1] (blocker) kat8 postCommitHook 'familjeratt_yttrande' is never dispatched — commit() reads no hook
  LOC: hubs_arende/lib/Service/ArendeService.php::commit (inside the verified-receipt block, after the provenance/dnr/retention flip at ~line 1453)
  EXPECT: Inside the `if (($kvitto['verifierad'] ?? false) === true)` block, after the provenance/dnr/retention flip, dispatch typ->getPostCommitHook() via HOOK-INFRA. For 'familjeratt_yttrande' prepare/register the tingsrätt-yttrande as a handling via EdiariumPort::registrera (handlingstyp from typ->getDhpHandlingstyp() = 'familjeratt_akt', riktning='upprattad'). MUST be best-effort/graceful — a failing post-hook logs a warning but NEVER fells the already-verified kvitto (same discipline as the existing advanceStegEfterCommit gap24 comment). The hook runs only once per verified commit (it sits inside the verified branch, and the idempotent re-POST path returns receiptFromRegistered() before reaching it, so a re-commit does not re-fire it).
  VERIFY: Smoke: extend Smoke.php with arendeTyp='familjeratt' — create, commit, assert the post-hook ran (EdiariumStub::getDiarium has the yttrande handling) and the commit kvitto is still verified. phpunit: assert that a thrown exception inside the post-hook does NOT propagate out of commit() and the verified kvitto is returned unchanged (graceful), and that a second commit() (idempotent path) does not re-fire the hook.
  DEPENDS: HOOK-INFRA

[MODUL-FAILCLOSED] (major) Empty frends_modul (null='inherit') silently defaults to hardcoded 'ifo_barn' at commit (consolidates KOMPL-02 + VERK-3)
  LOC: hubs_arende/lib/Service/FacksystemCommitService.php::resolveModul (line 168-171) + DEFAULT_MODUL (line 50)
  EXPECT: An ArendeTyp with frendsModul=null means 'inherit the host/beslut case's modul' (komplettering line 254, verkstallighet line 329 comments), NOT 'use a clinical default'. Today resolveModul coerces empty modul -> DEFAULT_MODUL='ifo_barn', silently routing e.g. an LSS/ek_bistand uppföljning into the ifo_barn Treserva-modul (a felrouting = sekretessincident per spec §7.1). FIX (fail-closed, defense-in-depth independent of the attach work): when commit is reached for a facksystem-committable destination and NO modul is resolvable from payload['frends_modul'], throw IntegrationException ('frends_modul saknas — inherit-typ kräver värd-modul') rather than guessing. Remove the silent DEFAULT_MODUL fallback for null/inherit types, OR demote it to a guarded, logged last-resort that is never reached by frendsModul=null inherit-types. The inherited modul value for a true attach is threaded in by the (future, deferred) attach path via caller-supplied payload['frends_modul'] — never guessed here.
  VERIFY: phpunit on FacksystemCommitService: commit() with payload lacking frends_modul (and a facksystem destination) throws IntegrationException instead of returning a kvitto with modul='ifo_barn'. Regression: a commit WITH an explicit frends_modul still routes to that modul. Confirm orosanmalan/ekonomi (explicit non-null frendsModul) commits are unaffected.
  DEPENDS: -

[ORO-1] (major) kat1 pliktGrind fas-spärr (skyddsbedömning blockerar stepper) is never enforced on the write path
  LOC: hubs_arende/lib/Service/ArendeLifecycleService.php::transitionera (the single legal step-mover, after the ALLOWED_TRANSITIONS edge check at ~line 103)
  EXPECT: When typ->getPliktGrind() === true AND franSteg === 'forhandsbedomning' AND nyttSteg === 'utredning', the move MUST be blocked (InvalidArgumentException -> controller 400) UNLESS a skyddsbedömnings-kvittens is present. Keep it config-driven (read pliktGrind from the registry row, NO if(kategori===1) branch). The move forhandsbedomning->avslutat ('inte inleda utredning') stays ALLOWED ungated. CRITICAL nuance found in code: the engine stores no kvittens column (Db/Arende.php has none), and pliktForArende (ArendeService.php:1738) derives 'skyddsbedömning gjord' from `steg not in [inflode,forhandsbedomning]` — which is CIRCULAR for a forhandsbedomning->utredning gate (the very move being gated is what would flip the derivation). So the gate needs an EXPLICIT kvittens signal, not steg-derivation: accept it via the existing transitionera $kontext arg (e.g. $kontext['skyddsbedomningKvitterad'] === true, passed by the frontend after the handläggare kvitterar), or add a lightweight kvittens marker (flagga row / boolean) that a skyddsbedömnings-commit sets. Do not reuse the circular steg-derivation.
  VERIFY: phpunit/Smoke: assert forhandsbedomning->utredning is BLOCKED for orosanmalan before the kvittens and ALLOWED after the kvittens signal is supplied; assert forhandsbedomning->avslutat stays allowed ungated; assert a pliktGrind=false type (e.g. ekonomi) is never gated. NOTE: Smoke.php step 8 currently transitions forhandsbedomning->utredning and EXPECTS success with no skyddsbedömning — that step must be updated to supply the kvittens (or it will correctly start failing), which is the regression lock.
  DEPENDS: -

[AB-01] (major) kat2 ansokan_bistand insats sub-typ router (5-way mål-enhet + frends_modul) never implemented
  LOC: hubs_arende/lib/Service/ArendeService.php::buildEntity (enhet, ~line 1802-1836) + commit (frends_modul, line 1401-1402); ArendeTypRegistry.php:205 (comment only)
  EXPECT: ansokan_bistand must resolve an insats sub-typ and route BOTH the mål-enhet (aldreomsorg@/lss@/vuxen@/ekb@/barn-familj@) AND the frends_modul (ao/lss/ifo_vuxen/ek_bistand/ifo_barn) together — the spec's single distinguishing särlogik for kat2 (§3.1/§3.2/Δ3). Today only a single hardcoded frendsModul='ifo_vuxen' + defaultEnhet='mottagning@' exist; there is no sub-typ input, no enhet map, no modul map. FIX within the config-not-code rule: carry an insatsTyp on the inflow rad (rad['insatsTyp'] from the SDK-kuvert/triage) and add a deterministic insatsTyp -> {enhet, frendsModul} map (cleanest as a JSON column subTypRouting on the ansokan_bistand registry row), read in buildEntity (enhet) and commit (frends_modul), falling back to the row defaults when no sub-typ is supplied so enhet and modul stay PAIRED through one map. This also subsumes AB-03 (the 'mottagning@' vs 'ifo_vuxen' divergence is the pre-router fallback, re-homed together by this router). Lower priority than the hooks/fail-closed items — sequence after Δ1/Δ7. Confirm with the team whether insatsTyp is actually available on the inflow before building (the inflow source is the only open question; the map itself is deterministic).
  VERIFY: phpunit: a parametrised test over the 5 insatsTyp values asserts buildEntity sets the matching enhet AND commit's payload['frends_modul'] is the paired modul; assert that NO insatsTyp falls back to (mottagning@, ifo_vuxen) defaults. Smoke: create ansokan_bistand with each insatsTyp and assert routing pairing.
  DEPENDS: -

=== BY_DESIGN (6) ===
- computeFristDue returns null whenever fristPolicy.speglasUrTreserva=true (ansokan_bistand AB-02, ekonomi EKO-01, vard_samverkan VS-02 frist-half, rattsligt_tvang RT-4, verkstallighet VERK-4, familjeratt FAM-5, komplettering KOMPL-05) :: Spec §6.5/GAP-018: the frist is mirrored from Treserva via Frends; the engine deliberately keeps NO own clock. The '21'/'30'/'tidsbegransat'/'domstol' match-arms are unreachable for these rows precisely because the speglas-gate short-circuits first — intentional, not a missing-frist bug. fristPolicy.typ='koordinering' and 'arver' likewise return null by design.
- No special code path / no hook for ekonomi, ansokan_bistand, vard_samverkan, verkstallighet (AB-05, EKO-02/03/04/05, VS-03/04, VERK-5/6/7) :: Spec §0/§3.3 verdict 'en motor, en saga, N config-rader, ~3 hooks': these are pure-config rows realised on the unmodified saga. Seeded-but-unread fields (kopplingDefault, diariePlikt-bool, dhpHandlingstyp, sekretessGrund) are consumed by other layers (frontend Axel C / retention / ACL-mall) or are documentary; the engine producing no per-type branch is the design working as intended. Dubbelmärkning (akut_fara) is correctly a cross-cutting flag in InnehallsKlassService, not a type axis.
- Cases born in steg='forhandsbedomning' regardless of forstaAtgard (ekonomi EKO-05, vard_samverkan VS-03, rattsligt_tvang RT-3, verkstallighet VERK-2) :: fodelseSteg keys birth-step off commit_destination (gap23). 'steg' is the generic lifecycle stepper, ORTHOGONAL to forstaAtgard (surfaced separately via nastaAtgardForArende). There is no birthStep field on the config row for the code to contradict. For kat6 the appropriate birth step follows from the RT-1 reverse-order hook work, not a separate fix; VERK-2 is purely a downstream symptom of the (deferred) attach path — verifier downgraded it to NOT_A_GAP.
- Pure-attach types (komplettering, verkstallighet) being mintable as standalone cases via createCase (KOMPL-01, KOMPL-03, KOMPL-04, VERK-1's attach-rewire) :: Verifier REFUTED KOMPL-01/03/04: the spec's 'startar aldrig nytt ärende' rule lives on Axel C (kopplingDefault='hor_till') in the CLASSIFICATION/TRIAGE layer (ArendeMatchService + InnehallsKlassService, both [BYGGS], 'finns inte i koden'), which decides whether an attach ever reaches createCase. The engine is intentionally category-agnostic (§2.1/§6.2 'noll grenar i kod'); kat7 is architecturally identical to kat4, confirming uniform config rows. Empty-krets ACL for a born attach is documented fail-closed (deny-by-default), not a breach. NOTE: VERK-1 was confirmed MISSING by its verifier (the attach FLOW itself is unbuilt [BYGGS]/Δ1) — but building it is a structural classification-layer deliverable, NOT an engine guard; it is out of scope for this minimal 'one engine + hooks' contract and tracked as the broader Δ1 attach-path work, while the fail-open consequence (wrong modul) is fixed defensively by MODUL-FAILCLOSED.
- aclProfil string carried verbatim, ACL-mall semantics unrealised for all 8 profiles (vard_samverkan VS-01, rattsligt_tvang RT-5, verkstallighet VERK-5, familjeratt FAM-3 shallow half) :: GroupfolderClient::applyAcl only enables advanced ACL ({acl:1}) and logs the profile; the per-profile rules are spec-marked [BYGGS]/Δ5 (GAP-058 three-layer ACL §6.6). The engine carrying the string with no contradiction is the intended generic design — the unbuilt seam realisation is tracked as Δ5, not engine logic to 'fix' now. (VS-01 was classed MISSING/Δ5 by its verifier but is the same unbuilt ACL-mall seam — defer to Δ5, not this contract.)
- partsModell value 'flerpartsarende' (ASCII) vs spec prose 'flerpartsärende' (FAM-4) :: Internal-consistent stored identifier; registry tests assert the ASCII form and nothing compares against the å-form. Pure doc/spec naming drift, no behavioural effect.

=== DECISIONS (5) ===
- partsModell materialization for familjeratt 'flerpartsarende' (FAM-2): how deep to model multi-party state :: Verifier confirmed MISSING for the DATA-materialization scope only (no engine path reads getPartsModell; no parts ledger; no case-row column). The minimal honest build (read the field in createCase + record parties as enumerable state via a Member party/motpart role or a small parts table, treat objektRef as case-pseudonym) is implementable, BUT it touches data-model shape and is entangled with FAM-3. Needs a human call on the data model before building — do NOT bundle it with the kat8 post-hook (FAM-1), which stands alone.
- familjeratt party-separated ACL 'familjeratt_inre_sekretess' (FAM-3): strict partsåtskillnad where parents are counterparts :: Requires legal+architecture input (OSL 26 kap inre sekretess / partsinsyn): how parties map across the three ACL layers (case-tag ∩ Groupfolder ∩ Tables view, GAP-058) so one party cannot see the other's submissions. Felklassning = sekretessincident — must not be built unilaterally. Part of Δ5 ACL-mall work, gated on the FAM-2 data model decision.
- kat2 behörighetskontroll (god man/ombud/samtycke) as engine state (AB-06) and kat2/kat5 'diarieförs vid ärendestart' (AB-04) :: Whether a behörighets-markör/grind should exist as engine state (mirroring the kat1 pliktGrind precedent) and whether facksystem-types need a distinct engine-side registration step at case start (vs being satisfied by the facksystem commit) are verksamhets-/arkiv-juridiska questions (PuB/dnr-serie/DHP), not code contradictions. Need human decision before adding behaviour.
- komplettering 'tillförs befintligt dnr' realisation (KOMPL-07) and kat6 commit-destination routing of 'diarium'/'e_arkiv' to EdiariumPort (RT-2) :: KOMPL-07: how 'tillförs befintligt dnr' is realised (append-handling vs own row) touches dnr-serier and the facksystem-integration contract (GAP-019). RT-2: the verifier REFUTED routing 'diarium' through EdiariumPort at the COMMIT boundary as speculative — once RT-1's pre-hook makes the case born 'registrerad', commit() short-circuits to receiptFromRegistered and never mis-routes. A CommitRouter->EdiariumPort branch is only worth adding if a kat6/e_arkiv case can legitimately reach commit unregistered; confirm that path exists before building, otherwise leave the config allowlist as a defined fallback.
- Per-kommun funktionsadresser: defaultEnhet='ekonomi@' vs matrix 'ekb@' (EKO-06), and the kategori->enhet routing-regel (§6.3 [BYGGS]) :: Which literal funktionsadress each kategori routes to is a per-kommun config/routing decision; the names are the ACL boundary (must match a real NC group) and the matrix labels were illustrative. Needs human confirmation of the kommun's actual funktionsadresser before treating any string as canonical — not a code bug.