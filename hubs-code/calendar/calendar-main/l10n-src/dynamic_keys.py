"""Dynamic translation keys that cannot be extracted automatically.

Add entries here for strings that are constructed at runtime or loaded
from the database. Each entry should be a t() or n() call so xgettext
can find it.

Example:
    DYNAMIC_KEYS = [
        t("Dynamic status message"),
        n("One item", "Multiple items"),
    ]
"""

DYNAMIC_KEYS = [
]
